# JSON Encoded Logic

See `docs/` folder for detailed information about this library and it's features.

```elixir
# Run tests
mix test

# Run benchmarks
mix run bench/benchmark.exs
mix run bench/benchmark_large.exs
```
